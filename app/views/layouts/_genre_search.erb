<!--部分テンプレート：ジャンル検索-->
<!--public homes#top items#index items#show-->

<table class = "table table-borderless">
  <thead class = "border text-center">
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody class = "border">
    <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name,items_path(genre_id: genre.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>